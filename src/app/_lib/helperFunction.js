import React from "react";

const helperFunction = () => {
  return (
    <div>
      This file will not be the part of routing. Because its parent folder name
      is starting with _(underscore). Inside these types of folder we can write
      separate UI logics or helper functions etc.
    </div>
  );
};

export default helperFunction;
